[project]
name = "subconv"
version = "2.0.0"
requires-python = ">=3.13"
dependencies = [
    "fastapi==0.111.0",
    "httpx==0.27.0",
    "PyYAML==6.0.1",
    "uvicorn==0.30.1",
    "pydantic-settings-yaml==0.2.0",
]

[dependency-groups]
build = [
    "nuitka",
]

[tool.uv]
package = false
